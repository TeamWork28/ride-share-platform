apiVersion: apps/v1
kind: Deployment
metadata: 
  name: booking-service
  namespace: ride-share
spec:
  replicas: 3
  selector:
      labels:
        app: booking-service
  template:
    metadata:
      matchLabels:
        app: booking-service
    spec:
      containers:
        - name: booking-service
          image: 628270103657.dkr.ecr.us-east-2.amazonaws.com/ride-share/booking-service:latest
          ports:
            - containerPort: 3001
          env:
            - name: MONGO_HOST
              valueFrom:
                configMapKeyRef:
                  name: ride-share-platform
                  key: MONGO_HOST
            - name: MONGO_PORT
              valueFrom:
                configMapKeyRef:
                  name: ride-share-platform
                  key: MONGO_PORT
            - name: MONGO_DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: ride-share-platform
                  key: MONGO_DB_NAME
            - name: MONGO_USER
              value: "admin"
            - name: MONGO_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ride-share-mongodb-secrets
                  key: MONGO_PASSWORD
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 5